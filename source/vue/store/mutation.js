export default {
    // 更新状态
    priv (state , data) {
        state.priv = data;
    } ,

    route (state , data) {
        state.route = data;
    } ,

    menu (state , data) {
        state.menu = data;
    } ,
};